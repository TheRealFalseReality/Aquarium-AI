# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - run: flutter pub get
      - run: flutter build web --dart-define=RECAPTCHA_V3_SITE_KEY=6Lc6P4AsAAAAANB5aWATibw1Aada7IUq6IttLI5a --dart-define=FACEBOOK_CLIENT_TOKEN=${{ secrets.FACEBOOK_CLIENT_TOKEN }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FISHAI_31D40 }}'
          channelId: live
          projectId: fishai-31d40
          entryPoint: "./" # This should point to the root of your flutter project
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks