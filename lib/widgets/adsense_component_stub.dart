import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';

// Stub versions for non-web platforms
class AdSenseBannerWidget extends StatelessWidget {
  const AdSenseBannerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

class AdSenseDisplayWidget extends StatelessWidget {
  const AdSenseDisplayWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}